<!-- <small class="pull-right">
  <span class="glyphicon glyphicon-pencil"></span>
  <%= t('activerecord.attributes.manual.metadata.updated') %> <%= l(manual.updated_at) %>
</small> -->

<hr>

<p><span class="glyphicon glyphicon-time"></span> <%= t('activerecord.attributes.manual.metadata.created_at') %>: <%= l(manual.created_at) %></p>
<p><span class="glyphicon glyphicon-user"></span> <%= t('activerecord.attributes.manual.metadata.author') %>: <%= link_to manual.user.global_name, user_path(manual.user) %></p>
<p><span class="glyphicon glyphicon-check"></span> <%= t('activerecord.models.category.one') %>: <%= link_to translation_by_slug(manual.category.slug), category_path(manual.category) %></p>
<%= render 'manuals/completed_steps_metadata', manual: manual if current_user.present? %>
